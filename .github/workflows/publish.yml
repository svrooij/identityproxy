name: Test and Publish

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
  pull_request:
    branches:
      - main

jobs:
  test-container:
    name: 🧪 Test Docker Container
    runs-on: ubuntu-latest
    steps:
      - name: 🧑‍💻 Checkout code
        uses: actions/checkout@v5

      - name: 👨‍🔧 Setup .NET Core SDK
        uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.x

      - name: 🐳 Build Docker image
        run: docker build -t svrooij/identityproxyapi:test -f ./IdentityProxy.Api/Dockerfile .
        working-directory: ./src

      - name: 📦 Restore packages
        run: dotnet restore

      - name: 🔨 Build solution
        run: dotnet build

      - name: 🧪 Run tests
        run: dotnet test

  build-docker:
    name: 📦 Push IdentityProxy to GitHub Container Registry
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/')
    needs: test-container
    permissions:
      contents: read
      packages: write
    steps:
      - name: 🧑‍💻 Checkout code
        uses: actions/checkout@v5
      
      - name: 🏗️ Set up Docker Buildx
        uses: docker/setup-buildx-action@v4.0.0
      
      - name: 🔑 Login to GitHub Container Registry
        uses: docker/login-action@v4.1.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      
      - name: 📃 Extract tag name
        id: tag_name
        run: echo "::set-output name=TAG_NAME::${GITHUB_REF#refs/tags/}"
      
      - name: 📃 Extract version from tag
        id: version
        run: echo "::set-output name=VERSION::${GITHUB_REF#refs/tags/v}"
      
      - name: 👷 Build and push
        uses: docker/build-push-action@v6
        with:
          context: ./src
          file: ./src/IdentityProxy.Api/Dockerfile
          platforms: linux/amd64,linux/arm64
          push: true
          build-args: |
            VERSION=${{ steps.version.outputs.VERSION }}
          tags: ghcr.io/${{ github.repository}}:${{ steps.tag_name.outputs.TAG_NAME }},ghcr.io/${{ github.repository}}:latest

  build-nuget:
    name: 📦 Push IdentityProxy to nuget.org
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    if: startsWith(github.ref, 'refs/tags/')
    needs: build-docker
    steps:
    - name: 👨‍💻 Check-out code
      uses: actions/checkout@v5

    - name: 👨‍🔧 Setup .NET Core SDK
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: |
          10.x
          8.x

    - name: 🔍 Enable problem matchers
      run: echo "::add-matcher::.github/matchers/dotnet.json"

    - name: 🔄 Update IdentityProxyBuilder.cs
      shell: pwsh
      run: |
        $tag = "${{ github.ref_name }}"
        $file = "./src/SvRooij.Testcontainers.IdentityProxy/IdentityProxyBuilder.cs"
        (Get-Content $file) -replace ':latest', ":$tag" | Set-Content $file
    
    - name: 🎒 Load packages
      run: dotnet restore ./src/SvRooij.Testcontainers.IdentityProxy/SvRooij.Testcontainers.IdentityProxy.csproj

    - name: 🛠️ Build code
      shell: pwsh
      run: dotnet build ./src/SvRooij.Testcontainers.IdentityProxy/SvRooij.Testcontainers.IdentityProxy.csproj --configuration Release --no-restore -p:Version=$("${{ github.ref_name }}".Substring(1))

    - name: 📦 Pack SvRooij.Testcontainers.IdentityProxy
      shell: pwsh
      run: dotnet pack ./src/SvRooij.Testcontainers.IdentityProxy/SvRooij.Testcontainers.IdentityProxy.csproj --configuration Release --no-build -p:Version=$("${{ github.ref_name }}".Substring(1))

    - name: 🔑 Nuget trusted publishing
      uses: NuGet/login@v1
      id: nuget_login
      with:
        user: svrooij

    - name: ✈️ Publish SvRooij.Testcontainers.IdentityProxy to nuget.org
      run: dotnet nuget push ./src/SvRooij.Testcontainers.IdentityProxy/**/SvRooij.Testcontainers.IdentityProxy.*.nupkg --api-key ${{steps.nuget_login.outputs.NUGET_API_KEY}} -s https://api.nuget.org/v3/index.json
